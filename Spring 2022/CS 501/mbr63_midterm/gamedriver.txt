Midterm Mangesh RautMbr63In game classI will use datatype string which is game name and game type and third variable as Int to get the number of players. In Game constructor I use default and parameterized constructor with parameter name type and number of players.I also use getters and setter to get and set the variables string and int.And last, I use toString method which print all the results together.In driver class which is my main class method createdI create list for game class which uses array list classParameter constructor for driver add all the games in list using the help of constructor.Recommend method is void which does not print anything but inside method it will recommend the game.Using for loop game list will print the game of boards using if else help and get type checks the board game using equals method.Random method chooses the random recommend game.